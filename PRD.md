# 求职看板 · 可复用版 PRD（v0，2026-08-27）

> 一页纸。之后所有「要不要加这个」拿它裁决。

## 一句话定位

上传简历、授权邮箱，每天拿到一张**按你的简历逐条对上 JD 原文**算出来的岗位看板——
**屏幕上每一个数字都能点开看它是从哪句话来的。**

## 为什么是这个定位（而不是"AI 帮你找工作"）

同类产品给的是一个说不清来处的匹配度。我们自己的板子犯过一模一样的错，
而且是被用户当场抓到的：分母是值班员手填的，70 个岗里 **0 个**能追溯到 JD 原文；
要求写得越少的岗分越高，一个没有 JD 的开放岗坐在榜首 100%；
而同一时期 **59 条投递、0 个面试邀请**。

所以差异化不是"更准"，是**可证伪**：命中几条 / 共几条、每条的 JD 原话、
算不出来就写「未核」而不是编一个数。

## 目标用户

第一批：**投递量已经 20+、但拿不到面试**的人。
他们不缺岗位列表，缺的是"为什么我投了没回音"这个问题的可核答案。
（不是刚开始找工作的人——他们还没有痛点数据。）

## 核心旅程

1. **上传简历** → 系统抽出一份「可举证的事实清单」（做过什么、几年、什么主语、什么学位），
   **交给用户逐条确认或改**。这份清单是之后所有判断的唯一基准，必须他本人点头。
2. **装本地组件**，用他自己已登录的浏览器读 LinkedIn 的个性化推荐页。
3. **每天一轮**：扫新岗 → 逐条抠 JD 的 Requirements 原文 → 对着事实清单判命中 →
   按「命中/总数」排序 → 输出增量（今天新增什么、关了什么）。
4. **授权邮箱** → 回执、拒信、面试邀约自动进台账；
   **「投了没人理」和「根本没查」必须分开显示**（渠道挂了要明说挂了）。
5. **一张固定链接的看板**，每天原地更新。

## 范围

**做**
- 简历 → 事实清单（用户确认）
- JD → 逐条要求（must / nice 由 JD 自己的段标题决定）
- 逐条命中判定 + 可点开的出处
- 排序、每日增量、关闭检测
- 邮箱回执 → 投递台账
- 「未核」是一等公民：核不出来就空着，不参与排名

**不做（明确划掉）**
- **代投 / 代填表单**——签的是用户名下的条款，责任在他不在我们
- **薪资估算**——实测 30 份 JD 里写薪酬数字的是 0 份，可得率为 0 的维度进了分数只能靠编
- **云端替用户登录 LinkedIn**——个性化页在登录墙后面，代登录违反条款且封的是**用户的**号
- **"帮你改简历"**——是另一个产品（OfferCoach），这里只做匹配

## 成功指标

**主指标：投递 → 面试邀请的转化率。**
不是 DAU、不是"分析了多少个岗"——那些数涨了而人还是拿不到面试，产品就是失败的。

**次指标（守住定位的那条）：可核率 = 屏幕上有分数的岗里，能逐条点开看出处的比例。**
目标 100%，低于 100% 的部分必须显示成「未核」。

## 现在有什么、缺什么

| 模块 | 状态 |
|---|---|
| JD → 逐条要求抽取（`extract_reqs.py`，154 行） | ✅ 已跑通，68 份真实 JD，抠出率 59/65 |
| 打分规则（`score.py`：must/nice 分开、无硬门槛单列、最小分母） | ✅ 已跑通，配 `gate-fit.py` 13 条断言 |
| 出处门禁（不许手填分母、判据不许按标题判序列） | ✅ `gate-fit.py` / `gate-noseq.py`，都证伪过 |
| 抓取（LinkedIn 个性化页 + JD 批量） | ✅ 已跑通（`batch.py` / `scan-foryou.py`） |
| 邮箱回执 → 台账 | ✅ 已跑通（Gmail + 163 两个渠道） |
| **简历 → 事实清单** | ❌ **唯一的真缺口**：现在是手写死一个人的（`judge_fit.py` 里 17 处「CV 上有/没有」） |
| 安装体验（不写代码的人装得上） | ❌ 现在要会用 Claude Code |

**所以第一步只有一件事：把 `judge_fit.py` 里那份写死的事实表，改成从上传的简历生成 + 用户确认。**
在那之前谈定价、谈网站、谈别的功能都是跑偏。

## 已知的最大风险

数据入口在登录墙后面，决定了这个东西**只能跑在用户自己的机器上**，不能是一个网站。
这不是工程选择，是合规红线。所有形态设计都要从这条出发。
