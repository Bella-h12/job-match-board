---
name: job-match-board
description: 把一页 LinkedIn 岗位列表变成一张按匹配度排序的求职看板。用户给出简历（文件或粘贴）+ LinkedIn 岗位页 URL 时使用：抓取每个岗位和公司的真实情报，逐条对照简历判出投中概率、优势、短板和该做什么，生成一个可交互的 HTML 看板（分档筛选 + 匹配度分数 + 状态标记）。触发词：求职看板、岗位匹配分析、帮我看这些岗位、job board、分析这页岗位、领英岗位分析。
---

# 求职看板 · Job Match Board

把「我该投哪些、按什么顺序、拿什么去打」变成一页可以每天看的东西。

产出不是一份泛泛的建议，而是一张对着**这个人的简历**和**这一页真实岗位**算出来的表：每个岗位有匹配度分数、投中概率、公司质量色灯、是否还在招，以及三栏「短板 / 怎么补 / 怎么切入」。

---

## 0 · 两个必需输入

**缺哪个就先要哪个，不要靠猜。**

1. **简历** —— 文件路径（PDF / DOCX / MD / TXT）或直接粘贴的正文。
   没有简历就没有匹配分析，只能做岗位罗列——这时明确告诉用户"我只能列岗位、判不了匹配"，让他选择是补简历还是接受降级版本。
2. **LinkedIn 岗位页 URL** —— 从 query string 里提取 **`currentJobId` + `originToLandingJobPostings`**（逗号分隔，URL-decode 后拆开）。这些 ID 是页面上的核心岗位，**是唯一可靠的数据锚点**：这类个性化列表页用关键词搜索复现不了。
   记下 `keywords` 和 `geoId` 用于后面顺带扫一遍相邻岗位。

第一次跑还要问清三件事，之后存进 `profile.json` 复用，别每天重问：

- **薪酬底线**（月薪或年薪，含币种）—— 没有底线就没法判"擦线"还是"远超"。
- **地点与远程偏好**（城市、可否搬迁、是否需要签证支持）。
- **在找什么方向**（职能序列，例如"产品 / 交付类，不要纯研究岗"）——这决定哪些岗位直接进"不投清单"。

---

## 1 · 读简历，先立标尺

在看任何岗位之前，先从简历里提炼出四样东西，写下来。后面每一条判断都引用它们，不要临时发挥：

| 提炼什么 | 用来干什么 |
|---|---|
| **能举证的经历**（做过什么、结果是什么、有没有数字） | 匹配时的"优势"栏 |
| **账面事实**（年限、职级、行业、学历、地点） | 对 JD 明写门槛，判"投中概率" |
| **真实短板**（缺的硬技能、缺的域知识、缺的年限） | "短板"栏，**必须诚实，不许美化** |
| **方向**（下一步想成为什么） | "对你的价值"评分 |

**年限一律用代码算，不要心算。** 拿当天日期和简历上的起止时间在 Python 里算出月数，把结果当事实用。模型不知道今天几号，也算不准跨年的年限——这是踩过的坑。

---

## 2 · 抓岗位数据

优先用已挂载的 linkedin MCP 工具（`mcp__linkedin__*`）。没挂载时用本 skill 自带的客户端直连：

```bash
cd <skill 目录>
python3 scripts/batch.py calls.json out.json     # 一个浏览器会话里跑完所有调用
```

`calls.json` 格式：`[{"tool":"get_job_details","args":{"job_id":"..."}}, ...]`
常用工具：`get_job_details {job_id}`、`get_company_profile {company_name, sections:"jobs"}`（company_name 是 LinkedIn slug）、`search_jobs {keywords, location, max_pages, date_posted}`。

抓取纪律（都是踩出来的）：

1. **岗位事实只信 `get_job_details`**，公司事实用 `get_company_profile`（员工数、两年增速、中位任期都在 about 页；中位任期 < 1.5 年是要写进报告的风险信号）。
2. **`search_jobs` 只用来顺带发现新岗**，不逐个深挖。它的结果里混大量竞价推广的无关岗（"AI product builder" 能搜出护士和夏令营教练），只捞标题和公司明显相关的条目列成线索。
3. **URL 里的岗位 ID 必须全抓，一个不落。** 个性化列表页搜索复现不了。
4. JD 页里的 **"People you can reach out to"、校友数、申请人数、申请人资历分布**是最值钱的增量情报，抓到就写进报告。
5. 同一家公司同一岗位多城市重复挂＝一个岗，报告里合并、标注只投一次。
6. 单次失败重试一次；连续失败改用公开 job 页兜底（`linkedin.com/jobs/view/<id>` 未登录也能看到 JD 正文）。

---

## 3 · 判读：每个岗位过同一套筛子

**先过硬门槛，直接筛掉的不浪费笔墨：**

- 薪酬估算低于用户的底线 → 进不投清单。
- 职能序列不在用户要的方向上 → 进不投清单（例如要做产品的人，不投要求博士的研究岗）。
- **招聘中介 / 派遣公司发布的岗位** → 单独归到"中介"档，任务是先问出真实雇主是谁再判断。JD 里出现 "our client" 是典型标志。

**剩下的按两个维度打分（各 5 点）：**

- **投中概率**＝账面资历对 JD 明写门槛的符合度 × 申请人数和竞争程度 × 是否有内推通道 × 发布时长。
- **对你的价值**＝对用户下一步方向的长期积累。前沿公司 > 生态大厂 > 本地企业，但**序列对不对比公司名气更重要**。

**再给一个 0–100 的匹配度分数**放进总表，和上面两个维度分开：分数只说岗位本身贴不贴这个人，公司好不好由色灯（🟢/🟡/🔴）单独表示。这两件事混在一起，中介发的完美 JD 会被顶到第一名。

**三栏必须逐条对着 JD 写，不许写套话：**

- **优势**：引用简历里能举证的经历，对上 JD 的具体某一条。JD 里点名的工具、方法、行业词，命中了就点出来。
- **短板**：明确区分是**硬门槛**（明写的 required）还是**加分项**（nice-to-have）。硬门槛就说是硬门槛，别安慰。
- **怎么切入**：一个具体动作，不是"优化简历"这种废话。

---

## 4 · 填 JSON，跑脚本出页面

**不要手写 HTML。** 把分析结果填成 `board.json`，交给构建脚本：

```bash
python3 scripts/build_board.py board.json out.html
```

字段照着 `examples/board.example.json` 填（那是一份能直接跑通的完整示例）。脚本会先校验再渲染：字段缺失、枚举写错、总表里公司重名、模板占位符没填满，都会带着出错位置报错停下，不会静默产出一个空段落。

几个容易写错的地方：

- `board[].light` 只能是 `green` / `yellow` / `red`；`board[].status` 只能是 `open` / `verify` / `filled` / `stale`。**今天真的逐个开页面确认过的才写 `open`**，沿用旧数据的写 `stale`，否则页面在骗人。
- `board[].tier` 用了哪几档，`tiers` 里就必须有对应说明。
- 富文本字段（`verdict`、`paras`、`strengths`、`gaps`、`steps`、`footer`、`tiers[].desc`）**支持内联 HTML**，可以用 `<b>` 和 `<a>`；纯文本字段（公司名、岗位名、地点等）会被自动转义。
- 每个岗位在**所有出现的位置**（排序表、深挖卡片、不投清单）都要带上直达 JD 的链接。

改完跑一次测试确认没弄坏：

```bash
python3 scripts/test_build.py
```

---

## 5 · 发布与跨天维护

- 用 Artifact 工具发布 `out.html`。**固定用同一个 artifact URL 更新**（传 `url` 参数），让用户的书签每天不变。
- `label` 用当天日期，方便回溯版本。
- **跨天时先读一遍现有页面**：已经分析过的岗位不重写全量拆解，只标注「已覆盖（M-D），状态变化：…」；只有新增岗做完整分析。
- **每天开工第一件事是复查旧待办的状态**，不是找新岗。一个上周就该投、今天确认还开着的好岗位，比三个新岗更重要——把这件事写进当天的核心结论里。

---

## 6 · 写字的规矩

这份东西是给人每天看的，不是给人看"AI 写得真花哨"的：

- **说人话。** 不用军事和游戏比喻（弹药、靶位、作战、赛道、打法），不用行业黑话。"你的优势""你的短板""怎么切入"就够了。
- **具体压过漂亮。** "1,462 人已投、53% 是资深候选人、挂了一个月"比"竞争激烈"有用一百倍。
- **不许编。** 薪酬是估算就写明是估算；没查证的状态标"待确认"；简历里没有的经历不要替用户吹出来。宁可写"这是真的门槛"。
- **结论先行。** 核心结论段直接说今天投什么、按什么顺序，把理由放到后面。
